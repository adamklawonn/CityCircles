#content.grid_12
  #midmap.grid_8.alpha
    = render :partial => 'maps/map', :locals => { :map => @map }
  #twitter.grid_4.omega
    #twtr-search-widget
    = javascript_include_tag "http://widgets.twimg.com/j/1/widget.js"
    = stylesheet_link_tag "http://widgets.twimg.com/j/1/widget.css"
    :javascript
      new TWTR.Widget({
        search: '_citycircles OR #citycircles',
        id: 'twtr-search-widget',
        loop: true,
        title: 'City Circles',
        subject: 'twittersphere',
        width: 300,
        height: 302,
        theme: {
          shell: {
            background: '#c7dacd',
            color: '#ffffff'
          },
          tweets: {
            background: '#ffffff',
            color: '#444444',
            links: '#1985b5'
          }
        }
      }).render().start();
  .clear
  #news.grid_4.alpha
    #infohead
    - if current_user
      = link_to_function image_tag( "smalladd.png", { :class => "buttonfloat", :border => 0 } ), "$j( '#postuniversalcontent' ).dialog( 'open' );$j( '#postuniversalcontent' ).dialog( 'option', 'position', [ 'center', 'center' ] );$( 'content_type' ).options[ 0 ].selected = true;", { :class => "tooltip", :title => "Post News" } 
    - else
      = link_to_function image_tag( "smalladd.png", { :class => "buttonfloat", :border => 0 } ), "$j( '#whoops' ).html( 'You must sign in to post content.' ).dialog( 'open' );", :class => "tooltip", :title => "Post News"
    %a{ :href => "#", :class => "tooltip", :title => "Subscribe to News Feed" }
      = image_tag "smallrss.png", { :class => "buttonfloat", :border => 0 }
    %h1
      news
      = image_tag "newscon.png", { :border => 0 }
    .infoblock
      - unless @news.empty?
        - @news.each do |news|
          %h2
            = link_to truncate( news.headline, :length => 30 ), news_url( news )
      - else
        %h2 No news...be the first to post!
      
 
  #events.grid_4
    - if current_user
      = link_to_function image_tag( "smalladd.png", { :class => "buttonfloat", :border => 0 } ), "$j( '#postuniversalcontent' ).dialog( 'open' );$j( '#postuniversalcontent' ).dialog( 'option', 'position', [ 'center', 'center' ] );$( 'content_type' ).options[ 1 ].selected = true;", { :class => "tooltip", :title => "Post Event" } 
    - else
      = link_to_function image_tag( "smalladd.png", { :class => "buttonfloat", :border => 0 } ), "$j( '#whoops' ).html( 'You must sign in to post content.' ).dialog( 'open' );", :class => "tooltip", :title => "Post Event"
    %a{ :href => "#", :class => "tooltip", :title => "Subscribe to Event Feed" }
      = image_tag "smallrss.png", { :class => "buttonfloat", :border => 0 }
    %h1
      events
      = image_tag "eventscon.png", { :border => 0 }
    .infoblock
      - unless @events.empty?
        - @events.each do |event|
          %h2
            = link_to truncate( event.headline, :length => 30 ), event_url( event )
      - else
        %h2 No events...be the first to post!
  #promos.grid_4.omega
    %a{ :href => "#" }
      = image_tag "smalladd.png", { :class => "buttonfloat", :border => 0 }
    %a{ :href => "#", :class => "tooltip", :title => "Subscribe to Business Promotions Feed" }
      = image_tag "smallrss.png", { :class => "buttonfloat", :border => 0 }
    %h1
      biz promos
      = image_tag "promocon.png", { :border => 0 }
    .infoblock
      %h2 No promos...be the first to post!
  #network.grid_4.alpha
    - if current_user
      = link_to_function image_tag( "smalladd.png", { :class => "buttonfloat", :border => 0 } ), "$j( '#postuniversalcontent' ).dialog( 'open' );$j( '#postuniversalcontent' ).dialog( 'option', 'position', [ 'center', 'center' ] );$( 'content_type' ).options[ 2 ].selected = true;", { :class => "tooltip", :title => "Post Network Item" } 
    - else
      = link_to_function image_tag( "smalladd.png", { :class => "buttonfloat", :border => 0 } ), "$j( '#whoops' ).html( 'You must sign in to post content.' ).dialog( 'open' );", :class => "tooltip", :title => "Post Network Item"
    %a{ :href => "#", :class => "tooltip", :title => "Subscribe to Network Feed" }
      = image_tag "smallrss.png", { :class => "buttonfloat", :border => 0 }
    %h1
      network
      = image_tag "networkcon.png", { :border => 0 }
    .infoblock
      - unless @networks.empty?
        - @networks.each do |network|
          %h2
            = link_to truncate( network.headline, :length => 30 ), network_url( network )
      - else
        %h2 No networks...be the first to post!
  #stuff.grid_4
    - if current_user
      = link_to_function image_tag( "smalladd.png", { :class => "buttonfloat", :border => 0 } ), "$j( '#postuniversalcontent' ).dialog( 'open' );$j( '#postuniversalcontent' ).dialog( 'option', 'position', [ 'center', 'center' ] );$( 'content_type' ).options[ 3 ].selected = true;", { :class => "tooltip", :title => "Post Your Stuff" } 
    - else
      = link_to_function image_tag( "smalladd.png", { :class => "buttonfloat", :border => 0 } ), "$j( '#whoops' ).html( 'You must sign in to post content.' ).dialog( 'open' );", :class => "tooltip", :title => "Post Your Stuff"
    %a{ :href => "#", :class => "tooltip", :title => "Subscribe to the Stuff Feed" }
      = image_tag "smallrss.png", { :class => "buttonfloat", :border => 0 }
    %h1
      stuff
      = image_tag "stuffcon.png", { :border => 0 }
    .infoblock
      - unless @stuffs.empty?
        - @stuffs.each do |stuff|
          %h2
            = link_to truncate( stuff.headline, :length => 30 ), stuff_url( stuff )
      - else
        %h2 No networks...be the first to post!
  .grid_4.omega
    - if current_user
      = link_to_function image_tag( "smalladd.png", { :class => "buttonfloat", :border => 0 } ), "$j( '#postuniversalcontent' ).dialog( 'open' );$j( '#postuniversalcontent' ).dialog( 'option', 'position', [ 'center', 'center' ] );$( 'content_type' ).options[ 4 ].selected = true;", { :class => "tooltip", :title => "Post Community Project" }
    - else
      = link_to_function image_tag( "smalladd.png", { :class => "buttonfloat", :border => 0 } ), "$j( '#whoops' ).html( 'You must sign in to post content.' ).dialog( 'open' );", :class => "tooltip", :title => "Post Community Project"
    %a{ :href => "#", :class => "tooltip", :title => "Subscribe to Community Projects Feed" }
      = image_tag "smallrss.png", { :class => "buttonfloat", :border => 0 }
    %h1
      let's fix it
      = image_tag "fixcon.png", { :border => 0 }
    #fixit.infoblock
      - unless @fix_its.empty?
        - @fix_its.each do |fix_it|
          %h2
            = link_to truncate( fix_it.headline, :length => 30 ), fix_it_url( fix_it )
      - else
        %h2 No networks...be the first to post!
  .clear

:javascript
  $j( "a.tooltip" ).tooltip( { track: true, delay: 0, showURL: false, showBody: " - ", fade: 250 } );
